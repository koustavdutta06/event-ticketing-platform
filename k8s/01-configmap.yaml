apiVersion: v1
kind: ConfigMap
metadata:
  name: ticketing-config
data:
  # Datasource URLs — DB names/hosts are not secret, only credentials are.
  CATALOG_DB_URL: jdbc:postgresql://catalog-postgres:5432/catalog_db
  INVENTORY_DB_URL: jdbc:postgresql://inventory-postgres:5432/inventory_db
  BOOKING_DB_URL: jdbc:postgresql://booking-postgres:5432/booking_db
  AUTH_DB_URL: jdbc:postgresql://auth-postgres:5432/auth_db

  # Messaging / cache
  KAFKA_BOOTSTRAP_SERVERS: kafka-0.kafka.ticketing.svc.cluster.local:9092
  REDIS_HOST: redis
  REDIS_PORT: "6379"

  # Inter-service URLs (ClusterIP service DNS names)
  CATALOG_SERVICE_URL: http://catalog-service:8081
  INVENTORY_SERVICE_URL: http://inventory-service:8082
  PAYMENT_SERVICE_URL: http://payment-service:8086

  # Misc
  JWT_EXPIRATION_MS: "3600000"
  RAZORPAY_CURRENCY: INR
  SMTP_HOST: smtp.resend.com
  SMTP_PORT: "587"
  SMTP_USERNAME: resend
